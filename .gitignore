# Python
.venv/
__pycache__/
*.py[cod]
*.pyd
*.egg-info/
build/
dist/

# App data (user-specific, not committed)
voices/*.wav
voices/*.ref.txt
voices/*.pkf
audio_cache/*.wav
audio_cache/*.mp3
library/*.age
library/.tmp/
launch_log.txt
*.log

# Tauri shell — Rust build outputs and auto-generated schemas (large + reproducible)
src-tauri/target/
src-tauri/gen/

# Retired PyInstaller sidecar binaries (multi-GB, replaced by launch-silent.vbs 2026-04-25)
src-tauri/binaries/

# Bundled CPython runtime (python-build-standalone, ~150 MB). Reproducible via
# runtime/fetch-python.ps1 — pinned tarball, not committed.
runtime/python/
runtime/*.tar.gz

# Tauri / Vite frontend build artifacts
node_modules/
dist-tauri/
dist-ssr/
*.local

# librosa cache (regenerable on next launch)
librosa-cache/

# Environment
.env

# Temp / debug files
pip_out.txt
pip_err.txt

# IDE / tooling
.claude/

# Video sub-project (Remotion / Node.js)
video/node_modules/
video/out/
video/frames/
video/.next/
video/dist/
*.mp4
*.mov
*.webm

# OS
.DS_Store
Thumbs.db
desktop.ini
