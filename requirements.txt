# Core
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
python-docx>=1.1.0
markdown>=3.6
numpy>=1.26.0
soundfile>=0.12.1
psutil>=6.0.0
platformdirs>=4.0.0

# Builtin TTS — Kokoro (offline, Apache 2.0)
kokoro>=0.9.2

# Voice cloning — F5-TTS (MIT). transformers pin: f5-tts 1.1.0 imports
# `from transformers import pipeline` via a code path that breaks under
# transformers >= 4.50 (internal restructure). Pinning to the last
# series confirmed to load cleanly. Re-evaluate when bumping f5-tts.
f5-tts>=1.1.0
torchaudio
transformers>=4.40,<4.50

# Whisper — forced-alignment for word timestamps on cloned audio
faster-whisper>=1.0.0

# Encrypted document library (simdad-crypto wraps pyrage + keyring).
# simdad-crypto is not on PyPI; the wheel is vendored at vendor/ and rebuilt
# from E:\Projects\SIM-DAD\simdad-crypto\ via `python -m build --wheel` whenever
# the upstream version bumps. This pattern matches simdad-crypto README §6.
./vendor/simdad_crypto-0.1.0-py3-none-any.whl
pyrage>=1.3.0
keyring>=25.0.0
